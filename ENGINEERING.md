# 工程规范

> 适用阶段：v0.x 设计文档期 + 后续开发期。远程仓库：Gitee。

---

## 分支策略

```
main            ← 稳定版本，仅从 dev 合并（禁止直接 push）
  └── dev       ← 日常开发主线，所有功能/模块/文档在此迭代
```

| 分支 | 用途 | 保护 |
|---|---|---|
| `main` | 可发布/可编译的稳定版本 | ✅ Gitee 分支保护 |
| `dev` | 日常开发，功能 + 文档全部在此迭代 | 建议保护 |

**只有两条分支。** 单人项目不需要 feature 分支的合并开销——所有工作直接在 `dev` 上进行。

### 工作节奏

```
dev 上迭代 ──→ 干净可编译 ──→ 合并到 main ──→ 打 tag
     ↑                                              │
     └──────────── 继续在 dev 上下一轮迭代 ←──────────┘
```

### 例外：实验性分支（按需拉，跑通即删）

当某个设计方向不确定、可能推倒重来时，临时拉一条分支做试验：

```
git checkout -b try/something dev
# ... 试验 ...
# 方案A: 跑通了 → 合回 dev → 删分支
# 方案B: 跑不通 → 直接删分支，dev 不受影响
```

这不是规范要求，是安全网——避免在 dev 上 `reset --hard`。

### 设计文档阶段（当前）

- 文档增改直接在 `dev` 上提交
- 跨文档一致化修改（如协议调整牵连 01/03/04），允许多个 commit 分批推
- 阶段里程碑（"设计冻结 v0.1"）：`dev` → `main` → `git tag v0.1`

### 代码开发阶段（Phase 2 起）

- 同样直接在 `dev` 开发
- **合并到 main 的门槛**：编译通过 + 核心测试通过 + 无已知崩溃路径

---

## Commit 格式

```
<type>(<scope>): <简短描述>
```

| type | 含义 | 示例 |
|---|---|---|
| `docs` | 文档新增/修改 | `docs(03): 新增 msg_revoke 和 msg_edit 帧定义` |
| `design` | 架构/协议/数据库设计决策 | `design(02): Worker Pool 粒度修正——单条直通` |
| `feat` | 新功能实现 | `feat(net): QWebSocket 断线重连状态机` |
| `fix` | Bug 修复 | `fix(ws): Hub broadcast 中读锁下写 map 的 race` |
| `refactor` | 重构（无功能变化） | `refactor(core): ConnectionManager 去 Singleton` |
| `chore` | 构建/工具/配置 | `chore: 更新 .gitignore 增加覆盖率文件` |

### 规则

- 描述用中文，type+scope 用英文
- 一行不超过 72 字符（方便 `git log --oneline`）
- **禁止** `WIP`、`fix bug`、`update` 等无信息量 commit
- 一个 commit 只做一件事

---

## 文档更新约定

1. **交叉引用必须可追溯**——引用其他文档时写文件名 + 章节号：`[03-通信协议](03-通信协议详细设计文档.md) §3.4.7`
2. **修订记录同步更新**——每份文档末尾有修订记录表格，修改后追加一条
3. **废弃内容用删除线标注**，不要直接删——保留设计决策的演化痕迹
4. **TODO 严禁留在 main**——所有 TODO 必须在合并前解决或转为 Issue

---

## 合并到 main 的自查清单（单人项目）

合并前在 dev 上自问：

- [ ] 编译通过（设计阶段此项为"文档无内部矛盾"）
- [ ] 无残留 TODO / 占位标记
- [ ] 无已知崩溃路径或死锁风险
- [ ] commit 历史干净（无 `fix typo` × 5 的噪音，必要时 `rebase -i` 压缩）

---

## 标签（Tag）规范

| Tag | 含义 |
|---|---|
| `v0.1` | 设计文档初版冻结 |
| `v0.2` | 设计文档修订版 |
| `v0.3` | 设计文档终版（Phase 1 结束） |
| `v1.0-alpha` | 首个可运行的客户端 |
| `v1.0` | 正式发布 |

---

## 提交前检查清单

- [ ] 文档无内部矛盾（交叉引用全部有效）
- [ ] 无残留 TODO 标记
- [ ] `.gitignore` 覆盖 IDE/OS 文件
- [ ] commit message 符合格式
- [ ] 已 rebase 到 Gitee 远程最新 `dev`（避免 force push）
